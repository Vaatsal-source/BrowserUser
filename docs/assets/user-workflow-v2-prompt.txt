Mode: built-in image_gen edit

Edit this Dev Privacy Guard User Workflow image. Preserve the same landscape canvas, layout, pastel colors, all arrows and their exact destinations, all unchanged nodes and wording. Make precisely these four improvements:
1. At the yellow cloud-shaped reasoning node near 66% x, replace cloud icon with a simple black server-rack outline icon. Replace bold heading "Cloud AI plans the next step" with "Self-hosted AI server" and add "Plans the next step" below it. Keep supporting text "Receives redacted context only" and external note "Raw screenshots are never sent". This is a self-hosted reasoning server, not a third-party LLM API. Fit legibly within the yellow node; modestly reshape that node into a rounded yellow server card if needed without moving its arrows.
2. In the upper-right purple diamond replace "Need your help?" with exact text "Human In The Loop (HITL)" using readable multiline bold text. Preserve person icon and Yes / No outgoing arrows.
3. In the first blue card under "Give it a goal", replace "Fill my scholarship application" with exact text "Fill this form".
4. In the lavender local-device group, move the explanatory note currently right of the database closer to that icon, immediately to its right with a small comfortable gap. Set the entire note in bold, exact text "Profile values stay on your device and are used when filling forms." Use 3 short lines to fit below the horizontal blue connector and within the lavender group. Keep database icon and Approved profile label. Do not overlap arrows or other content.
Preserve the important connector from Approved profile to Fill approved details, never to the reasoning server. All other content, layout and flow remain identical. Crisp high-resolution typography, no extra text.
